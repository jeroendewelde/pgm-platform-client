import React, { ReactElement } from 'react'

interface Props {
	
}

function CustomTextInput(): ReactElement {
	return (
		<>
			
		</>
	)
}

// export default CustomTagTextInput

export default function CustomTagTextInput({}: Props): ReactElement {
	return (
		<div>
			
		</div>
	)
}
